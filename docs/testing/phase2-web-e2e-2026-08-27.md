# Phase 2 Web E2E verification

Date: 2026-08-27
Scope: TASK-303

## Environment

- Next.js production server at http://localhost:3000.
- NestJS production build at http://localhost:4000.
- PostgreSQL 16.8 on localhost:5432.
- The Browser Use local-web-development procedure was followed: the Web
  server was rebuilt and the page was reloaded before verification.
- Temporary E2E deck, exam, attempts, mistakes, and tags were removed after
  the run.

## Journey results

- Login: passed with the local test account; the authenticated shell showed
  the admin user and protected navigation.
- Dashboard -> Flashcards -> detail -> back: passed; the created deck
  remained visible after returning to the library.
- Flashcard create/edit: passed; a deck and card were created, the card
  meaning was edited, and the updated value rendered without a manual page
  reload.
- FSRS review: passed; the new card was revealed, rated GOOD, and the queue
  changed from Due 1 / New 1 to Due 0 / New 0 with 100% progress.
- Exam library/detail: passed; a timed exam was opened from the library,
  edited, and reopened with its saved question, favorite, and tag.
- Timed exam and best-score cache: passed; a wrong answer produced a 0%
  official result, and returning to the library showed Best Score: 0%.
- Mistake review: passed; the submitted question appeared in Review mistakes
  and the Practice route opened successfully.
- Practice isolation: passed; a correct practice answer displayed the
  explicit no-official-best-score-changed result.
- No-answer-leakage: passed; live exam and practice DOM snapshots contained
  no isCorrect, correctOptionId, answerKey, or is_correct field names.
- Stale refresh: passed; after an external server-side description change, the
  warm detail view continued to show its cached value. After the 45-second
  cache TTL and a route remount, the new server value rendered and the old
  value disappeared.

## Regression fixed during verification

The exam editor used a non-existent PATCH /exams/{id}/metadata route. The
editor now uses the canonical PATCH /exams/{id} route documented by the API,
with a focused endpoint regression test in
apps/web/src/lib/exam-endpoints.spec.ts.
