# Phase 2 Cache and Browser-Storage Policy

Status: approved for Phase 2, effective 2026-08-26

## 1. Authority and goals

PostgreSQL/API remains the only authoritative store. Browser caches exist only to
make repeated reads and navigation feel fast. A cache miss, eviction, tab close,
network reconnect, or browser restart must never change domain behavior.

The policy uses an in-memory TanStack Query cache on Web and a bounded Room read
cache on Android. Neither cache is a sync queue or an offline write database.

## 2. Data classification

| Class                        | Examples                             | Web policy                                                  | Android policy                                       |
| ---------------------------- | ------------------------------------ | ----------------------------------------------------------- | ---------------------------------------------------- |
| Stable reference/list data   | folder tree, set/exam summaries      | memory query cache; 45s stale, 5m GC                        | Room read cache; bounded rows and age                |
| Normal entity detail         | set cards, exam management detail    | memory query cache; 45s stale, 5m GC                        | optional small detail cache                          |
| Search data                  | grouped set/card/exam/folder results | memory only; 20s stale, 2m GC, bounded query keys           | short-lived in-memory state; cancel/debounce         |
| Rapidly changing state       | mutation status, refresh indicators  | React/ViewModel state only                                  | ViewModel state only                                 |
| Live attempt state           | questions, selections, `expiresAt`   | query is session-scoped and freshness-first; no persistence | active attempt ID only in DataStore; refetch payload |
| Authentication/session state | bearer token and username            | current V1 localStorage token strategy; never query cached  | Keystore-encrypted token through DataStore           |
| UI-only preferences          | sort/filter/tab                      | versioned allow-list in localStorage, tiny values only      | DataStore if needed                                  |

Correct-answer metadata is management/result data only. It must never enter a live
attempt cache before grading, regardless of cache layer.

## 3. Web query defaults

The single application QueryClient uses:

- `staleTime: 45_000` ms for dashboard, lists, and entity details.
- `staleTime: 20_000` ms for search results.
- `gcTime: 300_000` ms for normal keys and `120_000` ms for search keys.
- No persistence plugin and no IndexedDB/localStorage query persister.
- One retry for network/5xx failures on reads; no retry for 4xx errors or writes.
- Abort obsolete reads through the `AbortSignal` supplied by the query function.
- Live attempts use `staleTime: 0`, `gcTime: session lifetime`, and explicit refetch
  on reconnect/visibility restore. Attempt responses are not persisted.

Query keys include every server-visible list/detail filter: page, page size,
search, sort, folder, favorite, and tag. Keys are generated by one module so two
components cannot accidentally create separate caches for the same request.

## 4. Mutation invalidation

| Mutation                                       | Invalidate/refetch                                                                       | Do not clear                              |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------- |
| Create/update/delete/duplicate set             | affected set detail, all set lists, dashboard, relevant search                           | exams/folders/live attempts               |
| Create/update/delete/reorder card              | affected set detail, set lists/counts, dashboard, relevant search, review counts         | unrelated sets/exams                      |
| Create/rename/move/delete folder               | folder tree, affected exam lists, dashboard/search                                       | flashcard queries/live attempts           |
| Create/update/delete/duplicate exam or content | affected exam detail/lists, folder tree if needed, dashboard/search, current best result | flashcard queries/live attempts           |
| Import one file                                | only the created domain's lists/detail/dashboard/search                                  | another file's import session             |
| Submit official exam                           | current exam best-result/detail/list and dashboard                                       | live attempt response before finalization |
| Submit FSRS review                             | affected card/review queue and dashboard due counts                                      | official exam best results                |

Successful small metadata mutations may update their entity cache directly, but
the corresponding list/count query is still invalidated. Failed mutations never
optimistically overwrite authoritative data.

## 5. Browser storage and cookie rules

Until secure cookie prerequisites exist for the current IP-only HTTP deployment,
Web keeps its V1 bearer-token strategy. The token is not placed in the QueryClient
or cookies; logout removes it and the username entry. No password, hash, signing
secret, raw Authorization header, API response body, Markdown, exam payload, or
correctness metadata is stored in browser storage.

The only new localStorage keys allowed are versioned UI preferences:

- `jsh_ui_preferences_v1.sort` — one of the documented sort values.
- `jsh_ui_preferences_v1.flashcard_search` — optional bounded string, max 120
  Unicode code points.
- `jsh_ui_preferences_v1.exam_folder` — UUID or empty string.
- `jsh_ui_preferences_v1.library_tab` — one of the documented tab values.

Implementations must remove invalid values, cap each value at 256 bytes, and keep
the allow-list finite. Do not create per-query localStorage keys. Cookies, if
introduced after HTTPS/CSRF approval, remain metadata-only and under 4 KB.

## 6. HTTP and server cache boundary

The browser's HTTP cache may revalidate authenticated non-live GETs only when the
API sends `Cache-Control: private, no-cache` (or an equivalent private policy) and
an `ETag`/`Last-Modified` validator. Public/shared caches are forbidden for
learning data. Live attempts, autosave responses, mutation responses, and auth
responses use `no-store` or freshness-first semantics.

The API currently has no shared server response cache. A future private server
cache must be bounded, keyed by authenticated user plus request parameters, and
must not replace query invalidation. CDN/public caching is not permitted.

## 7. Boundedness and observability

Search keys are capped at 30 recent distinct queries per tab and evicted by the
normal 2-minute GC policy. Normal query count is bounded by the active route and
5-minute GC. Android cache rows and age are bounded by its Room DAO policy.

Development tooling may inspect QueryClient cache key/count metadata, but must not
log response bodies, tokens, answer keys, or full learning content. Production
logs record cache misses/slow requests only as safe route metadata when enabled.
